// <auto-generated> 由 FeishuSdk.CodeGen 从 apis.json 生成；协议走 RequestPipeline，与手写服务一致。</auto-generated>
#nullable enable

using System.Runtime.CompilerServices;

namespace Feishu.Services.Application;

/// <summary>应用信息（application/v6）（生成代码：2 个端点）。</summary>
public sealed class ApplicationService
{
    private readonly RequestPipeline _pipeline;

    internal ApplicationService(RequestPipeline pipeline)
    {
        _pipeline = pipeline;
        Application = new ApplicationApplicationResource(pipeline);
    }

    /// <summary>Application 资源。</summary>
    public ApplicationApplicationResource Application { get; }

public sealed class ApplicationApplicationResource
{
    private readonly RequestPipeline _pipeline;

    internal ApplicationApplicationResource(RequestPipeline pipeline) => _pipeline = pipeline;

    /// <summary>获取应用信息（GET /open-apis/application/v6/applications/:app_id）。</summary>
    public Task<FeishuDataResponse> GetAsync(string appid, IDictionary<string, string>? query = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Get,
            Path = "/open-apis/application/v6/applications/:app_id",
            PathParams =
            {
                ["app_id"] = appid,
            },
            SupportedTokenTypes = [AccessTokenType.Tenant, AccessTokenType.User],
        };
        if (query != null)
            foreach (var kv in query)
                api.QueryParams.Add(kv.Key, kv.Value);
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>更新应用可用范围（管理员）（PATCH /open-apis/application/v6/applications/:app_id/admin）。</summary>
    public Task<FeishuDataResponse> PatchAdminAsync(string appid, object? body = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Patch,
            Path = "/open-apis/application/v6/applications/:app_id/admin",
            PathParams =
            {
                ["app_id"] = appid,
            },
            Body = body,
            SupportedTokenTypes = [AccessTokenType.Tenant],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

}

}
