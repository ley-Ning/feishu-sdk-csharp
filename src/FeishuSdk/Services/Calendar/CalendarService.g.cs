// <auto-generated> 由 FeishuSdk.CodeGen 从 apis.json 生成；协议走 RequestPipeline，与手写服务一致。</auto-generated>
#nullable enable

using System.Runtime.CompilerServices;

namespace Feishu.Services.Calendar;

/// <summary>日历（calendar/v4 高频端点）（生成代码：11 个端点）。</summary>
public sealed class CalendarService
{
    private readonly RequestPipeline _pipeline;

    internal CalendarService(RequestPipeline pipeline)
    {
        _pipeline = pipeline;
        Calendar = new CalendarCalendarResource(pipeline);
        Event = new CalendarEventResource(pipeline);
    }

    /// <summary>Calendar 资源。</summary>
    public CalendarCalendarResource Calendar { get; }
    /// <summary>Event 资源。</summary>
    public CalendarEventResource Event { get; }

public sealed class CalendarCalendarResource
{
    private readonly RequestPipeline _pipeline;

    internal CalendarCalendarResource(RequestPipeline pipeline) => _pipeline = pipeline;

    /// <summary>创建日历（POST /open-apis/calendar/v4/calendars）。</summary>
    public Task<FeishuDataResponse> CreateAsync(object? body = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Post,
            Path = "/open-apis/calendar/v4/calendars",
            Body = body,
            SupportedTokenTypes = [AccessTokenType.Tenant, AccessTokenType.User],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>获取日历信息（GET /open-apis/calendar/v4/calendars/:calendar_id）。</summary>
    public Task<FeishuDataResponse> GetAsync(string calendarid, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Get,
            Path = "/open-apis/calendar/v4/calendars/:calendar_id",
            PathParams =
            {
                ["calendar_id"] = calendarid,
            },
            SupportedTokenTypes = [AccessTokenType.Tenant, AccessTokenType.User],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>更新日历（PATCH /open-apis/calendar/v4/calendars/:calendar_id）。</summary>
    public Task<FeishuDataResponse> PatchAsync(string calendarid, object? body = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Patch,
            Path = "/open-apis/calendar/v4/calendars/:calendar_id",
            PathParams =
            {
                ["calendar_id"] = calendarid,
            },
            Body = body,
            SupportedTokenTypes = [AccessTokenType.Tenant, AccessTokenType.User],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>删除日历（DELETE /open-apis/calendar/v4/calendars/:calendar_id）。</summary>
    public Task<FeishuDataResponse> DeleteAsync(string calendarid, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Delete,
            Path = "/open-apis/calendar/v4/calendars/:calendar_id",
            PathParams =
            {
                ["calendar_id"] = calendarid,
            },
            SupportedTokenTypes = [AccessTokenType.Tenant, AccessTokenType.User],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>获取日历授权列表（分页）（GET /open-apis/calendar/v4/calendars/:calendar_id/acls）。</summary>
    public Task<FeishuDataResponse> ListAclAsync(string calendarid, IDictionary<string, string>? query = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Get,
            Path = "/open-apis/calendar/v4/calendars/:calendar_id/acls",
            PathParams =
            {
                ["calendar_id"] = calendarid,
            },
            SupportedTokenTypes = [AccessTokenType.Tenant, AccessTokenType.User],
        };
        if (query != null)
            foreach (var kv in query)
                api.QueryParams.Add(kv.Key, kv.Value);
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>获取日历授权列表（分页）：自动翻页枚举 data.items。</summary>
    public async IAsyncEnumerable<System.Text.Json.JsonElement> ListAclAsyncEnumerate(string calendarid, IDictionary<string, string>? query = null, RequestOptions? options = null, int? limit = null, [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var emitted = 0;
        string? pageToken = null;
        do
        {
            var pageQuery = query == null ? new Dictionary<string, string>() : new Dictionary<string, string>(query);
            if (pageToken != null) pageQuery["page_token"] = pageToken;
            var page = await ListAclAsync(calendarid, pageQuery, options, cancellationToken);
            page.EnsureSuccess();
            if (page.Data is not { ValueKind: System.Text.Json.JsonValueKind.Object } data) yield break;
            if (!data.TryGetProperty("items", out var items) || items.ValueKind != System.Text.Json.JsonValueKind.Array) yield break;
            foreach (var item in items.EnumerateArray())
            {
                if (limit.HasValue && emitted >= limit.Value) yield break;
                emitted++;
                yield return item;
            }
            pageToken = page.PageToken is { Length: > 0 } ? page.PageToken : null;
        } while (pageToken != null);
    }

    /// <summary>添加日历授权（POST /open-apis/calendar/v4/calendars/:calendar_id/acls）。</summary>
    public Task<FeishuDataResponse> CreateAclAsync(string calendarid, IDictionary<string, string>? query = null, object? body = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Post,
            Path = "/open-apis/calendar/v4/calendars/:calendar_id/acls",
            PathParams =
            {
                ["calendar_id"] = calendarid,
            },
            Body = body,
            SupportedTokenTypes = [AccessTokenType.Tenant, AccessTokenType.User],
        };
        if (query != null)
            foreach (var kv in query)
                api.QueryParams.Add(kv.Key, kv.Value);
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

}

public sealed class CalendarEventResource
{
    private readonly RequestPipeline _pipeline;

    internal CalendarEventResource(RequestPipeline pipeline) => _pipeline = pipeline;

    /// <summary>创建日历事件（POST /open-apis/calendar/v4/calendars/:calendar_id/events）。</summary>
    public Task<FeishuDataResponse> CreateAsync(string calendarid, IDictionary<string, string>? query = null, object? body = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Post,
            Path = "/open-apis/calendar/v4/calendars/:calendar_id/events",
            PathParams =
            {
                ["calendar_id"] = calendarid,
            },
            Body = body,
            SupportedTokenTypes = [AccessTokenType.Tenant, AccessTokenType.User],
        };
        if (query != null)
            foreach (var kv in query)
                api.QueryParams.Add(kv.Key, kv.Value);
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>获取日历事件（GET /open-apis/calendar/v4/calendars/:calendar_id/events/:event_id）。</summary>
    public Task<FeishuDataResponse> GetAsync(string calendarid, string eventid, IDictionary<string, string>? query = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Get,
            Path = "/open-apis/calendar/v4/calendars/:calendar_id/events/:event_id",
            PathParams =
            {
                ["calendar_id"] = calendarid,
                ["event_id"] = eventid,
            },
            SupportedTokenTypes = [AccessTokenType.Tenant, AccessTokenType.User],
        };
        if (query != null)
            foreach (var kv in query)
                api.QueryParams.Add(kv.Key, kv.Value);
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>更新日历事件（PATCH /open-apis/calendar/v4/calendars/:calendar_id/events/:event_id）。</summary>
    public Task<FeishuDataResponse> PatchAsync(string calendarid, string eventid, IDictionary<string, string>? query = null, object? body = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Patch,
            Path = "/open-apis/calendar/v4/calendars/:calendar_id/events/:event_id",
            PathParams =
            {
                ["calendar_id"] = calendarid,
                ["event_id"] = eventid,
            },
            Body = body,
            SupportedTokenTypes = [AccessTokenType.Tenant, AccessTokenType.User],
        };
        if (query != null)
            foreach (var kv in query)
                api.QueryParams.Add(kv.Key, kv.Value);
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>删除日历事件（DELETE /open-apis/calendar/v4/calendars/:calendar_id/events/:event_id）。</summary>
    public Task<FeishuDataResponse> DeleteAsync(string calendarid, string eventid, IDictionary<string, string>? query = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Delete,
            Path = "/open-apis/calendar/v4/calendars/:calendar_id/events/:event_id",
            PathParams =
            {
                ["calendar_id"] = calendarid,
                ["event_id"] = eventid,
            },
            SupportedTokenTypes = [AccessTokenType.Tenant, AccessTokenType.User],
        };
        if (query != null)
            foreach (var kv in query)
                api.QueryParams.Add(kv.Key, kv.Value);
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>查询日历事件（分页）（GET /open-apis/calendar/v4/calendars/:calendar_id/events）。</summary>
    public Task<FeishuDataResponse> ListAsync(string calendarid, IDictionary<string, string>? query = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Get,
            Path = "/open-apis/calendar/v4/calendars/:calendar_id/events",
            PathParams =
            {
                ["calendar_id"] = calendarid,
            },
            SupportedTokenTypes = [AccessTokenType.Tenant, AccessTokenType.User],
        };
        if (query != null)
            foreach (var kv in query)
                api.QueryParams.Add(kv.Key, kv.Value);
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>查询日历事件（分页）：自动翻页枚举 data.items。</summary>
    public async IAsyncEnumerable<System.Text.Json.JsonElement> ListAsyncEnumerate(string calendarid, IDictionary<string, string>? query = null, RequestOptions? options = null, int? limit = null, [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var emitted = 0;
        string? pageToken = null;
        do
        {
            var pageQuery = query == null ? new Dictionary<string, string>() : new Dictionary<string, string>(query);
            if (pageToken != null) pageQuery["page_token"] = pageToken;
            var page = await ListAsync(calendarid, pageQuery, options, cancellationToken);
            page.EnsureSuccess();
            if (page.Data is not { ValueKind: System.Text.Json.JsonValueKind.Object } data) yield break;
            if (!data.TryGetProperty("items", out var items) || items.ValueKind != System.Text.Json.JsonValueKind.Array) yield break;
            foreach (var item in items.EnumerateArray())
            {
                if (limit.HasValue && emitted >= limit.Value) yield break;
                emitted++;
                yield return item;
            }
            pageToken = page.PageToken is { Length: > 0 } ? page.PageToken : null;
        } while (pageToken != null);
    }

}

}
