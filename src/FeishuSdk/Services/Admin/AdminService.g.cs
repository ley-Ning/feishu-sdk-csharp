// <auto-generated> 由 FeishuSdk.CodeGen 从 apis.json 生成；协议走 RequestPipeline，与手写服务一致。</auto-generated>
#nullable enable

using System.Runtime.CompilerServices;

namespace Feishu.Services.Admin;

/// <summary>企业管理（admin/v1）（生成代码：14 个端点）。</summary>
public sealed class AdminService
{
    private readonly RequestPipeline _pipeline;

    internal AdminService(RequestPipeline pipeline)
    {
        _pipeline = pipeline;
        AdminDeptStat = new AdminAdminDeptStatResource(pipeline);
        AdminUserStat = new AdminAdminUserStatResource(pipeline);
        AuditInfo = new AdminAuditInfoResource(pipeline);
        Badge = new AdminBadgeResource(pipeline);
        BadgeGrant = new AdminBadgeGrantResource(pipeline);
        BadgeImage = new AdminBadgeImageResource(pipeline);
        Password = new AdminPasswordResource(pipeline);
    }

    /// <summary>AdminDeptStat 资源。</summary>
    public AdminAdminDeptStatResource AdminDeptStat { get; }
    /// <summary>AdminUserStat 资源。</summary>
    public AdminAdminUserStatResource AdminUserStat { get; }
    /// <summary>AuditInfo 资源。</summary>
    public AdminAuditInfoResource AuditInfo { get; }
    /// <summary>Badge 资源。</summary>
    public AdminBadgeResource Badge { get; }
    /// <summary>BadgeGrant 资源。</summary>
    public AdminBadgeGrantResource BadgeGrant { get; }
    /// <summary>BadgeImage 资源。</summary>
    public AdminBadgeImageResource BadgeImage { get; }
    /// <summary>Password 资源。</summary>
    public AdminPasswordResource Password { get; }

public sealed class AdminAdminDeptStatResource
{
    private readonly RequestPipeline _pipeline;

    internal AdminAdminDeptStatResource(RequestPipeline pipeline) => _pipeline = pipeline;

    /// <summary>部门维度用户活跃与功能使用数据（GET /open-apis/admin/v1/admin_dept_stats）。</summary>
    public Task<FeishuDataResponse> ListAsync(IDictionary<string, string>? query = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Get,
            Path = "/open-apis/admin/v1/admin_dept_stats",
            SupportedTokenTypes = [AccessTokenType.Tenant],
        };
        if (query != null)
            foreach (var kv in query)
                api.QueryParams.Add(kv.Key, kv.Value);
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>部门维度用户活跃与功能使用数据：自动翻页枚举 data.items。</summary>
    public async IAsyncEnumerable<System.Text.Json.JsonElement> ListAsyncEnumerate(IDictionary<string, string>? query = null, RequestOptions? options = null, int? limit = null, [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var emitted = 0;
        string? pageToken = null;
        do
        {
            var pageQuery = query == null ? new Dictionary<string, string>() : new Dictionary<string, string>(query);
            if (pageToken != null) pageQuery["page_token"] = pageToken;
            var page = await ListAsync(pageQuery, options, cancellationToken);
            page.EnsureSuccess();
            if (page.Data is not { ValueKind: System.Text.Json.JsonValueKind.Object } data) yield break;
            if (!data.TryGetProperty("items", out var items) || items.ValueKind != System.Text.Json.JsonValueKind.Array) yield break;
            foreach (var item in items.EnumerateArray())
            {
                if (limit.HasValue && emitted >= limit.Value) yield break;
                emitted++;
                yield return item;
            }
            pageToken = page.PageToken is { Length: > 0 } ? page.PageToken : null;
        } while (pageToken != null);
    }

}

public sealed class AdminAdminUserStatResource
{
    private readonly RequestPipeline _pipeline;

    internal AdminAdminUserStatResource(RequestPipeline pipeline) => _pipeline = pipeline;

    /// <summary>用户维度用户活跃与功能使用数据（GET /open-apis/admin/v1/admin_user_stats）。</summary>
    public Task<FeishuDataResponse> ListAsync(IDictionary<string, string>? query = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Get,
            Path = "/open-apis/admin/v1/admin_user_stats",
            SupportedTokenTypes = [AccessTokenType.Tenant],
        };
        if (query != null)
            foreach (var kv in query)
                api.QueryParams.Add(kv.Key, kv.Value);
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>用户维度用户活跃与功能使用数据：自动翻页枚举 data.items。</summary>
    public async IAsyncEnumerable<System.Text.Json.JsonElement> ListAsyncEnumerate(IDictionary<string, string>? query = null, RequestOptions? options = null, int? limit = null, [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var emitted = 0;
        string? pageToken = null;
        do
        {
            var pageQuery = query == null ? new Dictionary<string, string>() : new Dictionary<string, string>(query);
            if (pageToken != null) pageQuery["page_token"] = pageToken;
            var page = await ListAsync(pageQuery, options, cancellationToken);
            page.EnsureSuccess();
            if (page.Data is not { ValueKind: System.Text.Json.JsonValueKind.Object } data) yield break;
            if (!data.TryGetProperty("items", out var items) || items.ValueKind != System.Text.Json.JsonValueKind.Array) yield break;
            foreach (var item in items.EnumerateArray())
            {
                if (limit.HasValue && emitted >= limit.Value) yield break;
                emitted++;
                yield return item;
            }
            pageToken = page.PageToken is { Length: > 0 } ? page.PageToken : null;
        } while (pageToken != null);
    }

}

public sealed class AdminAuditInfoResource
{
    private readonly RequestPipeline _pipeline;

    internal AdminAuditInfoResource(RequestPipeline pipeline) => _pipeline = pipeline;

    /// <summary>用户行为日志搜索（GET /open-apis/admin/v1/audit_infos）。</summary>
    public Task<FeishuDataResponse> ListAsync(IDictionary<string, string>? query = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Get,
            Path = "/open-apis/admin/v1/audit_infos",
            SupportedTokenTypes = [AccessTokenType.Tenant],
        };
        if (query != null)
            foreach (var kv in query)
                api.QueryParams.Add(kv.Key, kv.Value);
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>用户行为日志搜索：自动翻页枚举 data.items。</summary>
    public async IAsyncEnumerable<System.Text.Json.JsonElement> ListAsyncEnumerate(IDictionary<string, string>? query = null, RequestOptions? options = null, int? limit = null, [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var emitted = 0;
        string? pageToken = null;
        do
        {
            var pageQuery = query == null ? new Dictionary<string, string>() : new Dictionary<string, string>(query);
            if (pageToken != null) pageQuery["page_token"] = pageToken;
            var page = await ListAsync(pageQuery, options, cancellationToken);
            page.EnsureSuccess();
            if (page.Data is not { ValueKind: System.Text.Json.JsonValueKind.Object } data) yield break;
            if (!data.TryGetProperty("items", out var items) || items.ValueKind != System.Text.Json.JsonValueKind.Array) yield break;
            foreach (var item in items.EnumerateArray())
            {
                if (limit.HasValue && emitted >= limit.Value) yield break;
                emitted++;
                yield return item;
            }
            pageToken = page.PageToken is { Length: > 0 } ? page.PageToken : null;
        } while (pageToken != null);
    }

}

public sealed class AdminBadgeResource
{
    private readonly RequestPipeline _pipeline;

    internal AdminBadgeResource(RequestPipeline pipeline) => _pipeline = pipeline;

    /// <summary>创建勋章（POST /open-apis/admin/v1/badges）。</summary>
    public Task<FeishuDataResponse> CreateAsync(object? body = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Post,
            Path = "/open-apis/admin/v1/badges",
            Body = body,
            SupportedTokenTypes = [AccessTokenType.Tenant],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>获取勋章详情（GET /open-apis/admin/v1/badges/:badge_id）。</summary>
    public Task<FeishuDataResponse> GetAsync(string badgeid, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Get,
            Path = "/open-apis/admin/v1/badges/:badge_id",
            PathParams =
            {
                ["badge_id"] = badgeid,
            },
            SupportedTokenTypes = [AccessTokenType.Tenant],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>获取勋章列表（GET /open-apis/admin/v1/badges）。</summary>
    public Task<FeishuDataResponse> ListAsync(IDictionary<string, string>? query = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Get,
            Path = "/open-apis/admin/v1/badges",
            SupportedTokenTypes = [AccessTokenType.Tenant],
        };
        if (query != null)
            foreach (var kv in query)
                api.QueryParams.Add(kv.Key, kv.Value);
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>获取勋章列表：自动翻页枚举 data.items。</summary>
    public async IAsyncEnumerable<System.Text.Json.JsonElement> ListAsyncEnumerate(IDictionary<string, string>? query = null, RequestOptions? options = null, int? limit = null, [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var emitted = 0;
        string? pageToken = null;
        do
        {
            var pageQuery = query == null ? new Dictionary<string, string>() : new Dictionary<string, string>(query);
            if (pageToken != null) pageQuery["page_token"] = pageToken;
            var page = await ListAsync(pageQuery, options, cancellationToken);
            page.EnsureSuccess();
            if (page.Data is not { ValueKind: System.Text.Json.JsonValueKind.Object } data) yield break;
            if (!data.TryGetProperty("items", out var items) || items.ValueKind != System.Text.Json.JsonValueKind.Array) yield break;
            foreach (var item in items.EnumerateArray())
            {
                if (limit.HasValue && emitted >= limit.Value) yield break;
                emitted++;
                yield return item;
            }
            pageToken = page.PageToken is { Length: > 0 } ? page.PageToken : null;
        } while (pageToken != null);
    }

    /// <summary>修改勋章信息（PUT /open-apis/admin/v1/badges/:badge_id）。</summary>
    public Task<FeishuDataResponse> UpdateAsync(string badgeid, object? body = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Put,
            Path = "/open-apis/admin/v1/badges/:badge_id",
            PathParams =
            {
                ["badge_id"] = badgeid,
            },
            Body = body,
            SupportedTokenTypes = [AccessTokenType.Tenant],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

}

public sealed class AdminBadgeGrantResource
{
    private readonly RequestPipeline _pipeline;

    internal AdminBadgeGrantResource(RequestPipeline pipeline) => _pipeline = pipeline;

    /// <summary>创建授予名单（POST /open-apis/admin/v1/badges/:badge_id/grants）。</summary>
    public Task<FeishuDataResponse> CreateAsync(string badgeid, object? body = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Post,
            Path = "/open-apis/admin/v1/badges/:badge_id/grants",
            PathParams =
            {
                ["badge_id"] = badgeid,
            },
            Body = body,
            SupportedTokenTypes = [AccessTokenType.Tenant],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>获取授予名单详情（GET /open-apis/admin/v1/badges/:badge_id/grants/:grant_id）。</summary>
    public Task<FeishuDataResponse> GetAsync(string badgeid, string grantid, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Get,
            Path = "/open-apis/admin/v1/badges/:badge_id/grants/:grant_id",
            PathParams =
            {
                ["badge_id"] = badgeid,
                ["grant_id"] = grantid,
            },
            SupportedTokenTypes = [AccessTokenType.Tenant],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>获取授予名单列表（GET /open-apis/admin/v1/badges/:badge_id/grants）。</summary>
    public Task<FeishuDataResponse> ListAsync(string badgeid, IDictionary<string, string>? query = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Get,
            Path = "/open-apis/admin/v1/badges/:badge_id/grants",
            PathParams =
            {
                ["badge_id"] = badgeid,
            },
            SupportedTokenTypes = [AccessTokenType.Tenant],
        };
        if (query != null)
            foreach (var kv in query)
                api.QueryParams.Add(kv.Key, kv.Value);
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>获取授予名单列表：自动翻页枚举 data.items。</summary>
    public async IAsyncEnumerable<System.Text.Json.JsonElement> ListAsyncEnumerate(string badgeid, IDictionary<string, string>? query = null, RequestOptions? options = null, int? limit = null, [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var emitted = 0;
        string? pageToken = null;
        do
        {
            var pageQuery = query == null ? new Dictionary<string, string>() : new Dictionary<string, string>(query);
            if (pageToken != null) pageQuery["page_token"] = pageToken;
            var page = await ListAsync(badgeid, pageQuery, options, cancellationToken);
            page.EnsureSuccess();
            if (page.Data is not { ValueKind: System.Text.Json.JsonValueKind.Object } data) yield break;
            if (!data.TryGetProperty("items", out var items) || items.ValueKind != System.Text.Json.JsonValueKind.Array) yield break;
            foreach (var item in items.EnumerateArray())
            {
                if (limit.HasValue && emitted >= limit.Value) yield break;
                emitted++;
                yield return item;
            }
            pageToken = page.PageToken is { Length: > 0 } ? page.PageToken : null;
        } while (pageToken != null);
    }

    /// <summary>修改授予名单（PUT /open-apis/admin/v1/badges/:badge_id/grants/:grant_id）。</summary>
    public Task<FeishuDataResponse> UpdateAsync(string badgeid, string grantid, object? body = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Put,
            Path = "/open-apis/admin/v1/badges/:badge_id/grants/:grant_id",
            PathParams =
            {
                ["badge_id"] = badgeid,
                ["grant_id"] = grantid,
            },
            Body = body,
            SupportedTokenTypes = [AccessTokenType.Tenant],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>删除授予名单（DELETE /open-apis/admin/v1/badges/:badge_id/grants/:grant_id）。</summary>
    public Task<FeishuDataResponse> DeleteAsync(string badgeid, string grantid, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Delete,
            Path = "/open-apis/admin/v1/badges/:badge_id/grants/:grant_id",
            PathParams =
            {
                ["badge_id"] = badgeid,
                ["grant_id"] = grantid,
            },
            SupportedTokenTypes = [AccessTokenType.Tenant],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

}

public sealed class AdminBadgeImageResource
{
    private readonly RequestPipeline _pipeline;

    internal AdminBadgeImageResource(RequestPipeline pipeline) => _pipeline = pipeline;

    /// <summary>上传勋章图片（POST /open-apis/admin/v1/badge_images）。</summary>
    public Task<FeishuDataResponse> CreateAsync(object? body = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Post,
            Path = "/open-apis/admin/v1/badge_images",
            Body = body,
            SupportedTokenTypes = [AccessTokenType.Tenant],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

}

public sealed class AdminPasswordResource
{
    private readonly RequestPipeline _pipeline;

    internal AdminPasswordResource(RequestPipeline pipeline) => _pipeline = pipeline;

    /// <summary>重置用户的企业邮箱密码（POST /open-apis/admin/v1/password/reset）。</summary>
    public Task<FeishuDataResponse> ResetAsync(object? body = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Post,
            Path = "/open-apis/admin/v1/password/reset",
            Body = body,
            SupportedTokenTypes = [AccessTokenType.Tenant],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

}

}
