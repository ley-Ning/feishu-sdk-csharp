// <auto-generated> 由 FeishuSdk.CodeGen 从 apis.json 生成；协议走 RequestPipeline，与手写服务一致。</auto-generated>
#nullable enable

using System.Runtime.CompilerServices;

namespace Feishu.Services.Passport;

/// <summary>账密安全（passport/v1）（生成代码：3 个端点）。</summary>
public sealed class PassportService
{
    private readonly RequestPipeline _pipeline;

    internal PassportService(RequestPipeline pipeline)
    {
        _pipeline = pipeline;
        Password = new PassportPasswordResource(pipeline);
        Session = new PassportSessionResource(pipeline);
    }

    /// <summary>Password 资源。</summary>
    public PassportPasswordResource Password { get; }
    /// <summary>Session 资源。</summary>
    public PassportSessionResource Session { get; }

public sealed class PassportPasswordResource
{
    private readonly RequestPipeline _pipeline;

    internal PassportPasswordResource(RequestPipeline pipeline) => _pipeline = pipeline;

    /// <summary>重置登录密码（PUT /open-apis/passport/v1/password）。</summary>
    public Task<FeishuDataResponse> UpdateAsync(object? body = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Put,
            Path = "/open-apis/passport/v1/password",
            Body = body,
            SupportedTokenTypes = [AccessTokenType.Tenant],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

}

public sealed class PassportSessionResource
{
    private readonly RequestPipeline _pipeline;

    internal PassportSessionResource(RequestPipeline pipeline) => _pipeline = pipeline;

    /// <summary>批量获取脱敏的用户登录信息（POST /open-apis/passport/v1/sessions/query）。</summary>
    public Task<FeishuDataResponse> QueryAsync(object? body = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Post,
            Path = "/open-apis/passport/v1/sessions/query",
            Body = body,
            SupportedTokenTypes = [AccessTokenType.Tenant],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>退出登录（POST /open-apis/passport/v1/sessions/logout）。</summary>
    public Task<FeishuDataResponse> LogoutAsync(object? body = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Post,
            Path = "/open-apis/passport/v1/sessions/logout",
            Body = body,
            SupportedTokenTypes = [AccessTokenType.Tenant],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

}

}
