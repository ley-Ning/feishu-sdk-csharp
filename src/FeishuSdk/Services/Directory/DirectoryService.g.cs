// <auto-generated> 由 FeishuSdk.CodeGen 从 apis.json 生成；协议走 RequestPipeline，与手写服务一致。</auto-generated>
#nullable enable

using System.Runtime.CompilerServices;

namespace Feishu.Services.Directory;

/// <summary>通讯录企业版（directory/v1）（生成代码：21 个端点）。</summary>
public sealed class DirectoryService
{
    private readonly RequestPipeline _pipeline;

    internal DirectoryService(RequestPipeline pipeline)
    {
        _pipeline = pipeline;
        CollaborationRule = new DirectoryCollaborationRuleResource(pipeline);
        CollaborationTenant = new DirectoryCollaborationTenantResource(pipeline);
        ShareEntity = new DirectoryShareEntityResource(pipeline);
        Department = new DirectoryDepartmentResource(pipeline);
        Employee = new DirectoryEmployeeResource(pipeline);
    }

    /// <summary>CollaborationRule 资源。</summary>
    public DirectoryCollaborationRuleResource CollaborationRule { get; }
    /// <summary>CollaborationTenant 资源。</summary>
    public DirectoryCollaborationTenantResource CollaborationTenant { get; }
    /// <summary>ShareEntity 资源。</summary>
    public DirectoryShareEntityResource ShareEntity { get; }
    /// <summary>Department 资源。</summary>
    public DirectoryDepartmentResource Department { get; }
    /// <summary>Employee 资源。</summary>
    public DirectoryEmployeeResource Employee { get; }

public sealed class DirectoryCollaborationRuleResource
{
    private readonly RequestPipeline _pipeline;

    internal DirectoryCollaborationRuleResource(RequestPipeline pipeline) => _pipeline = pipeline;

    /// <summary>新增可搜可见规则（POST /open-apis/directory/v1/collaboration_rules）。</summary>
    public Task<FeishuDataResponse> CreateAsync(object? body = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Post,
            Path = "/open-apis/directory/v1/collaboration_rules",
            Body = body,
            SupportedTokenTypes = [AccessTokenType.User, AccessTokenType.Tenant],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>查询可搜可见规则（GET /open-apis/directory/v1/collaboration_rules）。</summary>
    public Task<FeishuDataResponse> ListAsync(IDictionary<string, string>? query = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Get,
            Path = "/open-apis/directory/v1/collaboration_rules",
            SupportedTokenTypes = [AccessTokenType.User, AccessTokenType.Tenant],
        };
        if (query != null)
            foreach (var kv in query)
                api.QueryParams.Add(kv.Key, kv.Value);
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>查询可搜可见规则：自动翻页枚举 data.items。</summary>
    public async IAsyncEnumerable<System.Text.Json.JsonElement> ListAsyncEnumerate(IDictionary<string, string>? query = null, RequestOptions? options = null, int? limit = null, [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var emitted = 0;
        string? pageToken = null;
        do
        {
            var pageQuery = query == null ? new Dictionary<string, string>() : new Dictionary<string, string>(query);
            if (pageToken != null) pageQuery["page_token"] = pageToken;
            var page = await ListAsync(pageQuery, options, cancellationToken);
            page.EnsureSuccess();
            if (page.Data is not { ValueKind: System.Text.Json.JsonValueKind.Object } data) yield break;
            if (!data.TryGetProperty("items", out var items) || items.ValueKind != System.Text.Json.JsonValueKind.Array) yield break;
            foreach (var item in items.EnumerateArray())
            {
                if (limit.HasValue && emitted >= limit.Value) yield break;
                emitted++;
                yield return item;
            }
            pageToken = page.PageToken is { Length: > 0 } ? page.PageToken : null;
        } while (pageToken != null);
    }

    /// <summary>更新可搜可见规则（PUT /open-apis/directory/v1/collaboration_rules/:collaboration_rule_id）。</summary>
    public Task<FeishuDataResponse> UpdateAsync(string collaborationruleid, object? body = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Put,
            Path = "/open-apis/directory/v1/collaboration_rules/:collaboration_rule_id",
            PathParams =
            {
                ["collaboration_rule_id"] = collaborationruleid,
            },
            Body = body,
            SupportedTokenTypes = [AccessTokenType.User, AccessTokenType.Tenant],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>删除可搜可见规则（DELETE /open-apis/directory/v1/collaboration_rules/:collaboration_rule_id）。</summary>
    public Task<FeishuDataResponse> DeleteAsync(string collaborationruleid, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Delete,
            Path = "/open-apis/directory/v1/collaboration_rules/:collaboration_rule_id",
            PathParams =
            {
                ["collaboration_rule_id"] = collaborationruleid,
            },
            SupportedTokenTypes = [AccessTokenType.User, AccessTokenType.Tenant],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

}

public sealed class DirectoryCollaborationTenantResource
{
    private readonly RequestPipeline _pipeline;

    internal DirectoryCollaborationTenantResource(RequestPipeline pipeline) => _pipeline = pipeline;

    /// <summary>获取关联组织列表（GET /open-apis/directory/v1/collaboration_tenants）。</summary>
    public Task<FeishuDataResponse> ListAsync(RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Get,
            Path = "/open-apis/directory/v1/collaboration_tenants",
            SupportedTokenTypes = [AccessTokenType.User, AccessTokenType.Tenant],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

}

public sealed class DirectoryShareEntityResource
{
    private readonly RequestPipeline _pipeline;

    internal DirectoryShareEntityResource(RequestPipeline pipeline) => _pipeline = pipeline;

    /// <summary>获取共享成员范围（GET /open-apis/directory/v1/share_entities）。</summary>
    public Task<FeishuDataResponse> ListAsync(IDictionary<string, string>? query = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Get,
            Path = "/open-apis/directory/v1/share_entities",
            SupportedTokenTypes = [AccessTokenType.User, AccessTokenType.Tenant],
        };
        if (query != null)
            foreach (var kv in query)
                api.QueryParams.Add(kv.Key, kv.Value);
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>获取共享成员范围：自动翻页枚举 data.items。</summary>
    public async IAsyncEnumerable<System.Text.Json.JsonElement> ListAsyncEnumerate(IDictionary<string, string>? query = null, RequestOptions? options = null, int? limit = null, [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var emitted = 0;
        string? pageToken = null;
        do
        {
            var pageQuery = query == null ? new Dictionary<string, string>() : new Dictionary<string, string>(query);
            if (pageToken != null) pageQuery["page_token"] = pageToken;
            var page = await ListAsync(pageQuery, options, cancellationToken);
            page.EnsureSuccess();
            if (page.Data is not { ValueKind: System.Text.Json.JsonValueKind.Object } data) yield break;
            if (!data.TryGetProperty("items", out var items) || items.ValueKind != System.Text.Json.JsonValueKind.Array) yield break;
            foreach (var item in items.EnumerateArray())
            {
                if (limit.HasValue && emitted >= limit.Value) yield break;
                emitted++;
                yield return item;
            }
            pageToken = page.PageToken is { Length: > 0 } ? page.PageToken : null;
        } while (pageToken != null);
    }

}

public sealed class DirectoryDepartmentResource
{
    private readonly RequestPipeline _pipeline;

    internal DirectoryDepartmentResource(RequestPipeline pipeline) => _pipeline = pipeline;

    /// <summary>创建部门（POST /open-apis/directory/v1/departments）。</summary>
    public Task<FeishuDataResponse> CreateAsync(object? body = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Post,
            Path = "/open-apis/directory/v1/departments",
            Body = body,
            SupportedTokenTypes = [AccessTokenType.User, AccessTokenType.Tenant],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>更新部门（PATCH /open-apis/directory/v1/departments/:department_id）。</summary>
    public Task<FeishuDataResponse> PatchAsync(string departmentid, object? body = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Patch,
            Path = "/open-apis/directory/v1/departments/:department_id",
            PathParams =
            {
                ["department_id"] = departmentid,
            },
            Body = body,
            SupportedTokenTypes = [AccessTokenType.User, AccessTokenType.Tenant],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>删除部门（DELETE /open-apis/directory/v1/departments/:department_id）。</summary>
    public Task<FeishuDataResponse> DeleteAsync(string departmentid, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Delete,
            Path = "/open-apis/directory/v1/departments/:department_id",
            PathParams =
            {
                ["department_id"] = departmentid,
            },
            SupportedTokenTypes = [AccessTokenType.User, AccessTokenType.Tenant],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>获取部门列表（POST /open-apis/directory/v1/departments/filter）。</summary>
    public Task<FeishuDataResponse> FilterAsync(object? body = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Post,
            Path = "/open-apis/directory/v1/departments/filter",
            Body = body,
            SupportedTokenTypes = [AccessTokenType.User, AccessTokenType.Tenant],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>批量获取部门信息（POST /open-apis/directory/v1/departments/mget）。</summary>
    public Task<FeishuDataResponse> MgetAsync(object? body = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Post,
            Path = "/open-apis/directory/v1/departments/mget",
            Body = body,
            SupportedTokenTypes = [AccessTokenType.User, AccessTokenType.Tenant],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>搜索部门（POST /open-apis/directory/v1/departments/search）。</summary>
    public Task<FeishuDataResponse> SearchAsync(object? body = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Post,
            Path = "/open-apis/directory/v1/departments/search",
            Body = body,
            SupportedTokenTypes = [AccessTokenType.User, AccessTokenType.Tenant],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

}

public sealed class DirectoryEmployeeResource
{
    private readonly RequestPipeline _pipeline;

    internal DirectoryEmployeeResource(RequestPipeline pipeline) => _pipeline = pipeline;

    /// <summary>创建员工（POST /open-apis/directory/v1/employees）。</summary>
    public Task<FeishuDataResponse> CreateAsync(object? body = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Post,
            Path = "/open-apis/directory/v1/employees",
            Body = body,
            SupportedTokenTypes = [AccessTokenType.User, AccessTokenType.Tenant],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>更新员工信息（PATCH /open-apis/directory/v1/employees/:employee_id）。</summary>
    public Task<FeishuDataResponse> PatchAsync(string employeeid, object? body = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Patch,
            Path = "/open-apis/directory/v1/employees/:employee_id",
            PathParams =
            {
                ["employee_id"] = employeeid,
            },
            Body = body,
            SupportedTokenTypes = [AccessTokenType.User, AccessTokenType.Tenant],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>离职员工（DELETE /open-apis/directory/v1/employees/:employee_id）。</summary>
    public Task<FeishuDataResponse> DeleteAsync(string employeeid, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Delete,
            Path = "/open-apis/directory/v1/employees/:employee_id",
            PathParams =
            {
                ["employee_id"] = employeeid,
            },
            SupportedTokenTypes = [AccessTokenType.User, AccessTokenType.Tenant],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>批量获取员工列表（POST /open-apis/directory/v1/employees/filter）。</summary>
    public Task<FeishuDataResponse> FilterAsync(object? body = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Post,
            Path = "/open-apis/directory/v1/employees/filter",
            Body = body,
            SupportedTokenTypes = [AccessTokenType.User, AccessTokenType.Tenant],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>批量获取员工信息（POST /open-apis/directory/v1/employees/mget）。</summary>
    public Task<FeishuDataResponse> MgetAsync(object? body = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Post,
            Path = "/open-apis/directory/v1/employees/mget",
            Body = body,
            SupportedTokenTypes = [AccessTokenType.User, AccessTokenType.Tenant],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>搜索员工信息（POST /open-apis/directory/v1/employees/search）。</summary>
    public Task<FeishuDataResponse> SearchAsync(object? body = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Post,
            Path = "/open-apis/directory/v1/employees/search",
            Body = body,
            SupportedTokenTypes = [AccessTokenType.User, AccessTokenType.Tenant],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>待离职更新为在职（PATCH /open-apis/directory/v1/employees/:employee_id/regular）。</summary>
    public Task<FeishuDataResponse> RegularAsync(string employeeid, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Patch,
            Path = "/open-apis/directory/v1/employees/:employee_id/regular",
            PathParams =
            {
                ["employee_id"] = employeeid,
            },
            SupportedTokenTypes = [AccessTokenType.User, AccessTokenType.Tenant],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>恢复离职员工（POST /open-apis/directory/v1/employees/:employee_id/resurrect）。</summary>
    public Task<FeishuDataResponse> ResurrectAsync(string employeeid, object? body = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Post,
            Path = "/open-apis/directory/v1/employees/:employee_id/resurrect",
            PathParams =
            {
                ["employee_id"] = employeeid,
            },
            Body = body,
            SupportedTokenTypes = [AccessTokenType.User, AccessTokenType.Tenant],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>在职更新为待离职（PATCH /open-apis/directory/v1/employees/:employee_id/to_be_resigned）。</summary>
    public Task<FeishuDataResponse> ToBeResignedAsync(string employeeid, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Patch,
            Path = "/open-apis/directory/v1/employees/:employee_id/to_be_resigned",
            PathParams =
            {
                ["employee_id"] = employeeid,
            },
            SupportedTokenTypes = [AccessTokenType.User, AccessTokenType.Tenant],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

}

}
