// <auto-generated> 由 FeishuSdk.CodeGen 从 apis.json 生成；协议走 RequestPipeline，与手写服务一致。</auto-generated>
#nullable enable

using System.Runtime.CompilerServices;

namespace Feishu.Services.Docx;

/// <summary>文档（docx/v1 高频端点）（生成代码：5 个端点）。</summary>
public sealed class DocxService
{
    private readonly RequestPipeline _pipeline;

    internal DocxService(RequestPipeline pipeline)
    {
        _pipeline = pipeline;
        Document = new DocxDocumentResource(pipeline);
    }

    /// <summary>Document 资源。</summary>
    public DocxDocumentResource Document { get; }

public sealed class DocxDocumentResource
{
    private readonly RequestPipeline _pipeline;

    internal DocxDocumentResource(RequestPipeline pipeline) => _pipeline = pipeline;

    /// <summary>创建文档（POST /open-apis/docx/v1/documents）。</summary>
    public Task<FeishuDataResponse> CreateAsync(object? body = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Post,
            Path = "/open-apis/docx/v1/documents",
            Body = body,
            SupportedTokenTypes = [AccessTokenType.Tenant, AccessTokenType.User],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>获取文档基本信息（GET /open-apis/docx/v1/documents/:document_id）。</summary>
    public Task<FeishuDataResponse> GetAsync(string documentid, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Get,
            Path = "/open-apis/docx/v1/documents/:document_id",
            PathParams =
            {
                ["document_id"] = documentid,
            },
            SupportedTokenTypes = [AccessTokenType.Tenant, AccessTokenType.User],
        };
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>获取文档纯文本（GET /open-apis/docx/v1/documents/:document_id/raw_content）。</summary>
    public Task<FeishuDataResponse> GetRawContentAsync(string documentid, IDictionary<string, string>? query = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Get,
            Path = "/open-apis/docx/v1/documents/:document_id/raw_content",
            PathParams =
            {
                ["document_id"] = documentid,
            },
            SupportedTokenTypes = [AccessTokenType.Tenant, AccessTokenType.User],
        };
        if (query != null)
            foreach (var kv in query)
                api.QueryParams.Add(kv.Key, kv.Value);
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>获取文档块列表（分页）（GET /open-apis/docx/v1/documents/:document_id/blocks）。</summary>
    public Task<FeishuDataResponse> ListBlocksAsync(string documentid, IDictionary<string, string>? query = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Get,
            Path = "/open-apis/docx/v1/documents/:document_id/blocks",
            PathParams =
            {
                ["document_id"] = documentid,
            },
            SupportedTokenTypes = [AccessTokenType.Tenant, AccessTokenType.User],
        };
        if (query != null)
            foreach (var kv in query)
                api.QueryParams.Add(kv.Key, kv.Value);
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

    /// <summary>获取文档块列表（分页）：自动翻页枚举 data.items。</summary>
    public async IAsyncEnumerable<System.Text.Json.JsonElement> ListBlocksAsyncEnumerate(string documentid, IDictionary<string, string>? query = null, RequestOptions? options = null, int? limit = null, [EnumeratorCancellation] CancellationToken cancellationToken = default)
    {
        var emitted = 0;
        string? pageToken = null;
        do
        {
            var pageQuery = query == null ? new Dictionary<string, string>() : new Dictionary<string, string>(query);
            if (pageToken != null) pageQuery["page_token"] = pageToken;
            var page = await ListBlocksAsync(documentid, pageQuery, options, cancellationToken);
            page.EnsureSuccess();
            if (page.Data is not { ValueKind: System.Text.Json.JsonValueKind.Object } data) yield break;
            if (!data.TryGetProperty("items", out var items) || items.ValueKind != System.Text.Json.JsonValueKind.Array) yield break;
            foreach (var item in items.EnumerateArray())
            {
                if (limit.HasValue && emitted >= limit.Value) yield break;
                emitted++;
                yield return item;
            }
            pageToken = page.PageToken is { Length: > 0 } ? page.PageToken : null;
        } while (pageToken != null);
    }

    /// <summary>创建文档块（POST /open-apis/docx/v1/documents/:document_id/blocks/:block_id/children）。</summary>
    public Task<FeishuDataResponse> CreateBlockChildrenAsync(string documentid, string blockid, IDictionary<string, string>? query = null, object? body = null, RequestOptions? options = null, CancellationToken cancellationToken = default)
    {
        var api = new ApiRequest
        {
            Method = HttpMethod.Post,
            Path = "/open-apis/docx/v1/documents/:document_id/blocks/:block_id/children",
            PathParams =
            {
                ["document_id"] = documentid,
                ["block_id"] = blockid,
            },
            Body = body,
            SupportedTokenTypes = [AccessTokenType.Tenant, AccessTokenType.User],
        };
        if (query != null)
            foreach (var kv in query)
                api.QueryParams.Add(kv.Key, kv.Value);
        return _pipeline.SendForAsync<FeishuDataResponse>(api, options, cancellationToken);
    }

}

}
